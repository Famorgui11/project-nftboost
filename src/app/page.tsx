import CardCollectors from "@/components/cardCollectors";
import Container from "@/components/container";

const Home = () => {
	return (
		<Container>
			<CardCollectors />
		</Container>
	);
};

export default Home;
